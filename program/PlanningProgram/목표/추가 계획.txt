진행도 바를 추가한다.
글꼴을 추가한다.
뒤로가기 (ctrl + z)를 추가한다.
여러개를 한번에 선택할수 있는 기능을 추가한다.
엔터로 계획 추가가 가능하게 한다.
드래그로 계획의 위치를 바꿀 수 있게 만든다.
목표를 폴더 이름이 아닌 파일 이름으로 정한다.
완료한 계획을 저장한다.
저장 폴더의 위치를 디랙토리가 아닌 파일의 아래로 바꾼다.
계획목록의 크기를 자유자재로 바꿀 수 있게 만든다.
불러오기를 목록으로 만든다.
불러오기 창에 새로 만들기 칸을 넣는다.
현재 계획 개수를 진행도 옆에 표시한다.(x/y)
완료한 계획 취소하기
exe파일을 만든다.
